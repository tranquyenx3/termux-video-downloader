#!/data/data/com.termux/files/usr/bin/bash
URL="${1:-}"
[ -n "$URL" ] || exit 0
# Pass URL to the single VDown queue/worker. No interactive menu is opened.
"$HOME/bin/vdown" --share "$URL" >/dev/null 2>&1 &
exit 0
